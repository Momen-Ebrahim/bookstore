import 'package:bookstore/loading_card.dart';
import 'package:flutter/material.dart';

class CustomLoadingSelectedBook extends StatelessWidget {
  const CustomLoadingSelectedBook({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return loadingSelctedBook();
  }
}
