class BookCardModels {
  var image, category, title, autherName, price, rating;

  BookCardModels(
      {required this.category,
      required this.title,
      required this.autherName,
      required this.price,
      required this.image,
      required this.rating});
}
